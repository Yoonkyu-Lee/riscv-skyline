# Build artifacts
*.o
*.elf
*.asm
*.raw
*.img
*.ppm
_build/
bin/

# Exception: kernel/vendor/ holds the original-starter demo.o that
# the demo binary links against (drawing-loop driver). It is checked
# in even though it's an .o file.
!kernel/vendor/
!kernel/vendor/*.o

# Editor / OS junk
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Python
__pycache__/
*.pyc

# Old historical staging area (no longer used)
tools/_vendor/

# Local capture scratch
docs/img/skyline.cast
